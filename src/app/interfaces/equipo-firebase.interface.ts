export interface EquipoFirebase {
    // Propiedades específicas de los datos de Firebase
    frase: string;
    instagram: string;
    nombre: string;
    subtitulo: string;
    url: string;
    // ... otras propiedades ...
  }